# SQ8 Dotfiles

NOTE: since its my config files they contain a lot of settings that only apply
to me. For example, git has a git-credential-oauth setup to work with my
selfhosted gitlab instance, unfortunately it was to much of a hazzle keeping
those parts of the config outside git, so they get to stay :)
