# SQ8 Dotfiles

## secrets.nix
`secrets.nix` is an untracked file with per-device/user information. A template
files exist in the form of `template.secrets.nix`. Rename it and replace the
values inside with your own.

